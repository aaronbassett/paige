# Overmind Environment Variables
# This file is sourced by Overmind before starting processes
# Copy to .overmind.env and fill in your values:
#   cp .overmind.env.example .overmind.env

# Project root directory (auto-detected by setup script)
PROJECT_ROOT=

# Backend Configuration
PROJECT_DIR=
HOST=127.0.0.1
PORT=3001
DATA_DIR=~/.paige/
CHROMADB_URL=http://localhost:8000

# Anthropic API Key (REQUIRED - replace with your key)
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# GitHub Token (optional, for GitHub API access)
# Get your token from: https://github.com/settings/tokens
GITHUB_TOKEN=

# Node Environment
NODE_ENV=development
