// MCP Lifecycle tools — paige_start_session, paige_end_session
// TDD stub — will be implemented in Phase 8

import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';

/** Registers lifecycle tools (paige_start_session, paige_end_session) on the MCP server. */
export function registerLifecycleTools(_server: McpServer): void {
  // Stub — tools will be registered during implementation
}
