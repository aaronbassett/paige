// Dashboard Flow 4: Learning materials for unaddressed knowledge gaps

import type { DashboardLearningMaterialsData } from '../../types/websocket.js';

/**
 * Finds unaddressed knowledge gaps and returns curated learning resources.
 * Skipped if no unaddressed gaps exist.
 */
export function assembleLearningMaterials(): Promise<DashboardLearningMaterialsData | null> {
  return Promise.reject(new Error('Not implemented'));
}
